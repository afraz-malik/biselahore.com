# No public env vars are currently required.
#
# CMS secrets (CMS_SESSION_SECRET, CMS_ADMIN_PASSWORD_HASH) live in a
# gitignored .env.local (dev) or .env.production.local (server) — never here.
# See docs/cms.md for setup instructions.
